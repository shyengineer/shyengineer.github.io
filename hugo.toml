baseURL = "https://shyengineer.github.io/"
languageCode = "ko-kr"
title = "ShyEngineer | 개발·물리·경제 블로그"
theme = "PaperMod"

# 검색 엔진 최적화(SEO)를 위한 기본 설정
enableRobotsTXT = true

[sitemap]
  changefreq = "daily"
  priority = 0.8
  filename = "sitemap.xml"

[outputs]
    home = ["HTML", "RSS", "JSON"]

[menu]
  [[menu.main]]
    identifier = "archive"
    name = "🗂️ Archives"
    url = "/archives/"
    weight = 10

  [[menu.main]]
    identifier = "categories"
    name = "🧩 Categories"
    url = "/categories/"
    weight = 20

  [[menu.main]]
    identifier = "tags"
    name = "🏷️ Tags"
    url = "/tags/"
    weight = 30

  [[menu.main]]
    identifier = "search"
    name = "🔍 Search"
    url = "/search/"
    weight = 40

[params]
    # --- [SEO & 글로벌 설정] ---
    env = "production"
    description = "물리학과 경제학을 공부하는 엔지니어의 기록입니다. C++, 물리 시뮬레이션, 계량 경제학을 다룹니다."
    author = "ShyEngineer"
    keywords = ["C++", "물리엔진", "경제시뮬레이션", "반도체공학", "GAA"]
    googleAnalytics = "G-672600DYET"
    math = true # LaTeX 수식 활성화

    # --- [UI/UX 최적화: SEO에 도움] ---
    ShowReadingTime = true      # 읽는 시간 표시
    ShowShareButtons = true     # 소셜 공유 버튼
    ShowCodeCopyButtons = true  # 코드 복사 버튼
    ShowBreadCrumbs = true      # 브레드크럼(경로) 표시
    ShowSearchButton = true     # 검색 버튼 (Ctrl + /)

    # --- [홈 정보 & 라벨] ---
    [params.homeInfoParams]
        Title = "공학적 사고로 세상을 해석합니다 👋"
        Content = "물리 법칙의 코드화부터 경제 시스템의 시뮬레이션까지, 깊이 있는 지식을 기록합니다."

    [params.label]
        text = "Shy Engineer"
        icon = "/Logo.webp"
        iconHeight = 35

    # --- [프로필 모드] ---
    [params.profileMode]
        enabled = true
        title = "ShyEngineer의 기술 저장소"
        subtitle = "C++ 기반 물리 엔진 설계 및 경제 모델링 전문가를 꿈꾸는 공간입니다."
        imageUrl = "https://github.com/shyengineer.png"
        imageWidth = 120
        imageHeight = 120

        [[params.profileMode.buttons]]
            name = "전체 글 보기"
            url = "posts"
[related]
  includeNewer = true
  threshold = 80  # 점수가 80점 이상인 글만 추천 (조절 가능)
  toLower = false

    [[related.indices]]
        name = "tags"
        weight = 100  # 태그 일치 시 가장 높은 가중치

    [[related.indices]]
        name = "categories"
        weight = 50   # 카테고리는 보조 가중치

    [[related.indices]]
        name = "date"
        weight = 10   # 최신글 가산점